{
  "name": "eco-server-package-api",
  "version": "1.0.0",
  "description": "Eco Package API",
  "scripts": {
    "start": "serverless offline",
    "devdb": "sls dynamodb start --migrate --stage dev",
    "deploy": "serverless deploy",
    "undeploy": "serverless remove"
  },
  "dependencies": {
    "@the-sett/serverless-elm-bridge": "^4.0.0"
  },
  "devDependencies": {}
}
